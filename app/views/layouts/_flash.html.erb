<% flash.each do |type, message| %>
  <div class="mb-4 rounded-md p-4 <%= type == 'notice' ? 'bg-green-50 text-green-800' : 'bg-red-50 text-red-800' %>">
    <p class="text-sm font-medium"><%= message %></p>
  </div>
<% end %>
